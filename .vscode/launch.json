{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ (inteprog_finals)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/inteprog_finals",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,  // This is the key to fix terminal input issues
            "MIMode": "lldb",
            "preLaunchTask": "build inteprog_finals"
        }
    ]
}
